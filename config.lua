--[[--------------------------------------------------------------------
-- Name: InfiniteBoost
-- Description: Instant recharge for vehicles with boost in FiveM.
-- Author: Disky
--]]--------------------------------------------------------------------

CONFIG = {}

CONFIG.usePermissions = true -- If the command should require the player to have the CONFIG.permissionObject permission.
CONFIG.permissionObject = "disky.coolcommands" -- The permission required for CONFIG.usePermissions.
CONFIG.spawnCommandsEnabled = false -- If the commands "spawnvoltic" & "spawnoppressor" should be registered/usable.